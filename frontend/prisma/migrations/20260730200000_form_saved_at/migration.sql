-- Отметка времени последнего сохранения формы клиники.
--
-- Нужна, чтобы «Собрать заново» после обрыва продолжало с места, а не шло
-- по кругу: случай, собранный позже этой отметки, уже отвечает текущим
-- данным клиники. На сотне пациентов это вдвое меньше вызовов модели,
-- а отказы провайдера — обычное дело.

ALTER TABLE "Organization" ADD COLUMN "formSavedAt" TIMESTAMP(3);
