-- Услуга с ценой по кнопке на экране звонка: в каких упражнениях она есть.
-- showsService=true у профилактики возражений, отработки возражений
-- и перехвата инициативы — они начинаются с середины разговора. Значения
-- проставляет сид типов тренировки, он же источник правды; здесь только
-- столбец со значением по умолчанию.
ALTER TABLE "TrainingType"
  ADD COLUMN "showsService" BOOLEAN NOT NULL DEFAULT false;
