-- Демо-доступ на считанные разговоры: один аккаунт собственнику до созвона.
-- demoTalksLimit: сколько завершённых разговоров с репликами даёт доступ.
--   null — прежнее суточное демо (или организация не демо вовсе).
-- У такого демо demoExpiresAt значит не конец суток, а момент исчерпания:
--   от него тикает та же неделя хвоста на вход и чтение разборов.
ALTER TABLE "Organization"
  ADD COLUMN "demoTalksLimit" INTEGER;
