-- Открывающий ход пациента: в каких упражнениях он заговаривает первым.
-- opensDialog=true у отработки возражений и перехвата инициативы — там
-- разговор ведёт пациент. Значения проставляет сид типов тренировки,
-- он же источник правды; здесь только столбец со значением по умолчанию.
ALTER TABLE "TrainingType"
  ADD COLUMN "opensDialog" BOOLEAN NOT NULL DEFAULT false;
