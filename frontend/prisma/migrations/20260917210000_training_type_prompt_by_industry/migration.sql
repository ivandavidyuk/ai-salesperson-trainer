-- Сцена упражнения по отрасли: у клиники «ты пришла в клинику и ждёшь
-- у стойки», у офиса продаж — «ждёшь у макета квартала». Ключ — отрасль
-- («недвижимость»), значение — текст промпта; пусто — берётся prompt.
ALTER TABLE "TrainingType" ADD COLUMN "promptByIndustry" JSONB NOT NULL DEFAULT '{}';
