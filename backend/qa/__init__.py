"""Проверка промптов пациентов: прогон, метрики, чтение моделью, сводка.

Инструмент **только докладывает**. Ни порогов, ни кода возврата, ни «не
прошло»: он показывает, что у пациента реплики по сорок секунд при медиане
двенадцать, а решаем мы. Порогов сейчас взять неоткуда, и выдуманный красный
вердикт быстро научил бы игнорировать инструмент целиком.

Зачем вообще: оба самых дорогих дефекта июля-августа нашлись чтением глазами
при отличных числах — немой Станислав (одиннадцать ходов ремарками, `paid 9.6`)
и монологи Оксаны по сорок секунд (`paid 7.4`). Боевой оценщик их не видит
по устройству: он судит менеджера, а ломается пациент.

Запуск — `python -m qa.run`, см. его модуль.
"""
