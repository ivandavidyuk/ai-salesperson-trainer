# Зависимости для разработки и тестов (в продакшен-образ не попадают).
# Установка: pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0

# Линтер. Заведён после того, как опечатка в имени переменной внутри
# WebSocket-цикла доехала до прода: тесты покрывают чистую логику и main.py
# не импортируют, поэтому необъявленное имя прошло CI насквозь.
ruff>=0.6
