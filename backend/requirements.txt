# Веб-фреймворк и ASGI-сервер
fastapi>=0.115
uvicorn[standard]>=0.30

# ElevenLabs Realtime STT (WebSocket-клиент)
websockets>=12

# OpenRouter LLM и ElevenLabs TTS (REST)
httpx>=0.27

# Хранилище состояния сессии
redis>=5.0
asyncpg>=0.30

# Проверка JWT (тот же секрет, что в Next.js)
PyJWT>=2.9

# Настройки из .env
pydantic-settings>=2.4
