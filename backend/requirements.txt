# Веб-фреймворк и ASGI-сервер
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12

# Yandex SpeechKit STT (gRPC) + готовые protobuf-стабы
grpcio>=1.66
yandexcloud>=0.300

# YandexGPT и TTS (REST)
httpx>=0.27

# Хранилище состояния сессии
redis>=5.0
asyncpg>=0.30

# Проверка JWT (тот же секрет, что в Next.js)
PyJWT>=2.9

# Настройки из .env
pydantic-settings>=2.4
