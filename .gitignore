# переменные окружения (секреты) — .example-шаблоны не игнорируются
.env
.env.local
.env.*.local

# системные файлы и логи
.DS_Store
*.log

# локальные настройки Claude Code (у каждого разработчика свои)
.claude/
